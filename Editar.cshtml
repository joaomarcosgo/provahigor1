@model ProvaHigorr.Models.Usuario
@{
    ViewData["Title"] = "Editar Perfil";
}
<h2 class="mb-4">Editar Perfil</h2>
<form asp-action="Editar" method="post" class="w-100 w-md-50 mx-auto p-4 border rounded bg-light">
    <div class="mb-3">
        <label class="form-label">Nome</label>
        <input asp-for="Nome" class="form-control" required />
    </div>
    <div class="mb-3">
        <label class="form-label">Login</label>
        <input asp-for="Login" class="form-control" readonly />
    </div>
    <div class="mb-3">
        <label class="form-label">Nova Senha</label>
        <input type="password" name="senha" class="form-control" />
        <small class="form-text text-muted">Deixe em branco para não alterar</small>
    </div>
    <button type="submit" class="btn btn-primary">Salvar</button>
</form>
@if (TempData["Mensagem"] != null)
{
    <div class="alert alert-success mt-3">@TempData["Mensagem"]</div>
}
